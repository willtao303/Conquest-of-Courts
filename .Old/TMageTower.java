/*
import java.awt.Color;

public class TMageTower extends TowerMagic{

    TMageTower(int x, int y) {
        super(x, y);
        this.color = new Color(167, 154, 252);
        this.UPGRADES = 2;
    }
    TMageTower(int x, int y, int hp){
        this(x, y);
        this.hp = hp;
    }
    @Override
    public int upgradeByButton(int id) {
        switch (id){
        }
        return -1;
    }
}
*/